# Architecture — unitypark/specd

> **status: DRAFT — review before trusting.** Generated by specd from a
> read-only repo scan. Claims the agent could not ground in the code are
> marked `UNVERIFIED`. Merging this PR is how you adopt it; edit freely.

## What specd is

A monorepo implementing a spec-driven delivery pipeline: `Connect → Ground → Spec → [human gate] → Build → Learn` (README). The README states the station line is fixed ("D11"), only station 01 takes configuration, and the gate at 04 is structural.

## Workspace layout

pnpm workspace (`pnpm-workspace.yaml`) over `packages/*` and `apps/*`. The `cli/` directory is a Go module and is **not** a pnpm workspace member — it is driven by root scripts.

| Path | Role (per README) | File count seen |
| --- | --- | --- |
| `apps/api` | NestJS API — auth, projects, pipeline, agents, knowledge index | 55 under `src/` |
| `apps/web` | Next.js — landing, wizard, dashboard, board, spec review, knowledge, runs | 13 `app/`, 9 `components/`, 2 `lib/` |
| `cli` | Go `specd` binary (`cmd/specd`, `internal/api`, `internal/config`) | 5 |
| `packages/shared` | Spec lifecycle, EARS rendering, model rate card, cost metering | 8 |
| `packages/db` | Drizzle schema + SQL migrations (Postgres + pgvector) | 4 src, 3 migrations |
| `packages/templates` | `AGENTS.md`, `CLAUDE.md`, `knowledge/` scaffold | 5 |

UNVERIFIED — no source file inside these directories was read. Every per-directory responsibility above is quoted from the README, not observed in code; check that `apps/api/src` is actually NestJS modules and that `packages/shared` exports what the table claims.

## Runtime dependencies

From `docker-compose.yml` (evidence):

| Service | Image | Host port |
| --- | --- | --- |
| postgres | `pgvector/pgvector:pg17` | 5433 → 5432 |

Dev credentials are `specd/specd`, database `specd`. It has a healthcheck; data lives in the named volume `specd-pgdata`.

Postgres is the only runtime dependency. This doc previously flagged `UNVERIFIED` that Redis/BullMQ — advertised in the README and provisioned in compose — might not be wired at all. That doubt was correct: nothing imported either, and both were removed (see `decisions/0008-remove-unused-queue.md`). Runner dispatch polls a `queued` row in `agent_runs`; it is not a queue.

## Ports and entry points

README: API on `:4000`, web on `:3000`. No entry point files were detected in the scan. UNVERIFIED — locate `apps/api/src/main.ts` (or equivalent) and the Next.js app entry to confirm ports and bootstrap.

## AI provider modes

README describes three paths ("§P3"):

1. `SPECD_AI_MODE=subscription_runner` — shells out to a locally logged-in Claude Code CLI; no credential is stored or proxied; no per-call billing, no schema guarantee (reply is shape-checked with one repair attempt).
2. `ANTHROPIC_API_KEY` — Messages API, schema-enforced, metered per token from a rate card in EUR integer cents.
3. Neither — onboarding still writes the template scaffold with every claim marked `UNVERIFIED`; spec generation fails loudly rather than inventing content.

UNVERIFIED — all of this is README prose. Check where `SPECD_AI_MODE` is read, what the shape-check/repair code path is, and where the rate card lives in `packages/shared`.

## Invariants claimed to be enforced in code

The README claims each of these is enforced by code plus a test. Treat the mechanism as UNVERIFIED until the named symbol is read.

| Invariant | Claimed enforcement point |
| --- | --- |
| Only a named human approves | `SpecsService.transition` rejects `approved` without an actor; state machine; DB CHECK constraint on approver |
| Gate cannot be bypassed | `specd spec pull` 409s server-side for unapproved; CLI tokens audience-scoped and rejected on author/approve routes |
| Approval is append-only | `approved → draft` refused; v2 supersedes v1, v1 keeps its stamp |
| Citations are checkable | Citations validated against retrieved content and against coverage: `supported` / `unsupported` / `unknown`. Invented docs and invented sections are demoted; a real doc that retrieval never surfaced is reported as unchecked rather than wrong |
| Loop closes | Last task of every spec files as-built copy to `knowledge/specs/`; appended if the model omits it |
| Spend capped | Caps checked before a run starts; cost in integer EUR cents |
| Agents never push | Build agent gets editing tools only; specd pushes only to the spec's branch, never a default branch |
| GitHub not impersonable | Unauthenticated webhook, HMAC over raw bytes in constant time before parse, unset secret rejects all, dedup by delivery id, repo+installation must match a project |
| GitLab not impersonable | Unauthenticated webhook, secret-token comparison (GitLab does not sign payloads) in constant time, unset secret rejects all, dedup by delivery id, repository match scoped to the sending instance (`project.web_url` vs. the connection's `instanceUrl`) so two instances sharing a numeric project id cannot cross-trigger each other's specs |

UNVERIFIED — the DB CHECK constraint should be visible in `packages/db/migrations`; the webhook HMAC and the spec state machine in `apps/api/src`. Verify each before relying on it. The GitLab row is grounded in `apps/api/src/vcs/gitlab-webhook.{service,verify}.ts` and `knowledge/decisions/0002-gitlab-via-personal-access-token.md` rather than a scan — read those directly rather than re-verifying by inference.

## Data model

UNVERIFIED — no schema file was read. `packages/db/src` holds a Drizzle schema and `packages/db/migrations` has 3 SQL migrations; entities implied by the README are projects, users/actors, specs (versioned, append-only), runs, knowledge/embedding index, and CLI tokens. Read `packages/db/src` before assuming any table or column.

## Trust boundaries (as described)

- CLI is a thin client ("D13"): fetches, registers, reports; never authors, reviews or approves.
- `specd login` is a device flow requiring the web app (`/cli-login`) for human confirmation; token stored in macOS keychain or `0600` under the config dir.
- Git is the source of truth for knowledge; the platform holds only a derived index (embeddings, metadata, run history).

UNVERIFIED — confirm token storage and audience scoping in `cli/internal/config` and `cli/internal/api`.

## Planning source

`SPEC-PLATFORM-PLAN.html` at the repo root is cited by the README as the spec being implemented (P1 scope, §14); section references like §9, §P3, D2, D11, D13 throughout the README point into it. Read it for design rationale — do not infer rationale from code.
