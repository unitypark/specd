# Conventions — unitypark/specd

> **status: DRAFT — review before trusting.** Generated by specd from a
> read-only repo scan. Claims the agent could not ground in the code are
> marked `UNVERIFIED`. Merging this PR is how you adopt it; edit freely.

## Commands (from root `package.json`)

| Task | Command |
| --- | --- |
| Install | `pnpm install` (pnpm 10.32.1 pinned via `packageManager`; Node >= 22) |
| Infra up/down | `pnpm infra:up` / `pnpm infra:down` (docker compose) |
| Migrate / seed | `pnpm db:migrate` (`@specd/db`) / `pnpm db:seed` (`@specd/api`) |
| Dev (both) | `pnpm dev` — parallel `@specd/api` + `@specd/web`; also `dev:api`, `dev:web` |
| Build | `pnpm build` — packages first, then api, then web (order is deliberate) |
| Typecheck | `pnpm typecheck` (`pnpm -r typecheck`) |
| Test | `pnpm test` = `pnpm -r test` + `pnpm cli:test` (`go vet ./... && go test ./...`) |
| CLI build/install | `pnpm cli:build` → `./bin/specd`; `pnpm cli:install` → `go install` + `scripts/check-cli-path.mjs` |
| Headless pipeline | `pnpm --filter @specd/api loop` |

Verify command for changes: `pnpm typecheck && pnpm test`.

## Package naming

Workspace packages are scoped `@specd/*` (`@specd/api`, `@specd/web`, `@specd/db`; `@specd/shared` and `@specd/templates` implied by the directory layout — UNVERIFIED, confirm the `name` field in each `package.json`).

## Testing

- Vitest is the root devDependency; per-package `test` scripts are run recursively. UNVERIFIED — no `vitest.config` was seen; confirm which packages actually define a `test` script and whether any use a different runner.
- Go tests for the CLI run under `cli:test` and are gated by `go vet` first.
- README: 183 tests; the `loop` run under `subscription_runner` reports "22 passed, 0 skipped".
- **Tests that need Postgres skip themselves when none is reachable** (gate + webhook tests) — the suite must stay green on a machine with no infra. Preserve that behaviour when adding integration tests.
- README states webhook signatures are tested with real HMAC and App JWTs with real RSA keys, deliberately not mocked. Follow that precedent for signing/verification code.

## TypeScript

`tsconfig.base.json` at the root is the shared base. UNVERIFIED — its compiler options (strictness, module resolution, path aliases) were not read; check before adding a package.

## Configuration

- `.env.example` is committed; `cp .env.example .env` and dev defaults work as-is. Keep new variables documented there.
- Known env vars from the README: `SPECD_AI_MODE` (`subscription_runner`), `ANTHROPIC_API_KEY`. UNVERIFIED — the full variable set is in `.env.example`, which was not shown.
- Postgres is on **5433** and Redis on **6380** on the host (non-default, to avoid clashing with local installs).

## Writing conventions the product itself enforces

These apply to generated content and to knowledge docs in this repo:

- Every design claim is either **cited** or flagged `UNVERIFIED`. A citation that cannot be followed is treated as worse than none.
- Citations are validated against what was actually retrieved; invented paths get demoted to `UNVERIFIED`.
- Skipped work is **labelled as skipped**, never silently passed (the headless `loop` does this for model-dependent steps).
- Money is integer EUR cents — no floats for spend.

## Behavioural rules for agents working in this repo

- Read `knowledge/` before writing code; work from an approved spec; write what was built back to `knowledge/specs/` in the same PR.
- Never push to a default branch. Work lands on the spec's own branch plus a PR (hosted) or a branch to diff (local mode).
- `packages/templates` ships `AGENTS.md` and `CLAUDE.md` scaffolds — UNVERIFIED, check whether those templates also govern this repo or only onboarded projects.

## Git hygiene

129 tracked files; `.gitignore` present. UNVERIFIED — its contents were not read; `bin/specd` is a build output and presumably ignored, confirm before committing.
